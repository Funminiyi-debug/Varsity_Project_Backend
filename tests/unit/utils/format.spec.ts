import { NextFunction, Request, Response } from "express";
import productFormat from "../../../src/utils/formatProduct_Service";
import "jest";

describe("format product data into acceptable format to be received on client", () => {
  it("Should return next function", () => {
    // const data = {
    //   _id: "id-1",
    //   name: "name1",
    //   otherFields: { a: "a", b: "b" },
    // };
    // const result = productFormat(data);
    // console.log(result);
  });
});
